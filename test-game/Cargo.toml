[package]
name = "test-game"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
engine = {path="../engine"}
render = {path="../render"}
glam = "0.30.9"
lazy_static = "1.5.0"
glfw = {workspace = true}

[build.dependencies]
engine = {path="../engine"}
